import { Box, Typography } from "@mui/material";

export default function TablePage() {
  return (
    <Box sx={{ minHeight: "calc(100vh - (32px + 0px))", color: "white" }}>
      <Typography color="white">this is table page sdfs</Typography>
    </Box>
  );
}
